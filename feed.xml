<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://www.dinhphu28.com/feed.xml" rel="self" type="application/atom+xml"/><link href="https://www.dinhphu28.com/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-04-17T15:39:18+00:00</updated><id>https://www.dinhphu28.com/feed.xml</id><title type="html">Dinh Phu Nguyen</title><subtitle>Dinh Phu Nguyen also know as dinhphu28 is a Software Engineer with high level experience in Java - Spring Boot, RESTful Web Service and software development. </subtitle><entry><title type="html">Java Virtual Threads Explained: How They Work and When to Use Them</title><link href="https://www.dinhphu28.com/blog/2026/java-virtual-threads-explained-how-they-work-and-when-to-use-them/" rel="alternate" type="text/html" title="Java Virtual Threads Explained: How They Work and When to Use Them"/><published>2026-04-17T09:19:00+00:00</published><updated>2026-04-17T09:19:00+00:00</updated><id>https://www.dinhphu28.com/blog/2026/java-virtual-threads-explained-how-they-work-and-when-to-use-them</id><content type="html" xml:base="https://www.dinhphu28.com/blog/2026/java-virtual-threads-explained-how-they-work-and-when-to-use-them/"><![CDATA[<blockquote> <p>This post continues from <a href="/blog/2026/why-blocking-io-hurts-and-how-asynchronous-fixes-it/">Part 1: Why Blocking I/O Hurts and How Asynchronous Fixes It</a></p> </blockquote> <h2 id="introduction">Introduction</h2> <p>Official release in Java 21, <strong>Virtual Threads</strong> are a lightweight, software-based thread managed by the Java Virtual Machine (JVM) instead of the OS.</p> <p>It allows us to write code in a “linear” synchronous style while getting the high-scale performance of complex asynchronous code.</p> <h2 id="how-virtual-thread-works">How Virtual Thread works</h2> <p>Instead of running on the CPU, Virtual Threads run on top of a small pool of standard OS threads called Carrier Threads usually one per CPU core.</p> <h3 id="mounting-and-unmounting">Mounting and Unmounting</h3> <ul> <li><strong>Mounting</strong>: When a Virtual Thread has work to do, the JVM mounts it into a Carrier Thread, which then executes tasks on the CPU.</li> <li><strong>Unmounting</strong>: When tasks hit a blocking operation (I/O), the JVM unmounts the Virtual Thread from the Carrier Thread. Virtual Thread’s state (stack and variables) will be captured and moved from Carrier Thread to the Java Heap (RAM). The Carrier Thread is now free to process other Virtual Threads.</li> </ul> <p>Once the I/O operation finishes, the JVM parks the Virtual Thread back into a queue. As soon as a Carrier Thread becomes available, the JVM restores the state from RAM and Virtual Thread continues exactly where it left off.</p> <h3 id="why-its-better-os-thread">Why it’s better OS thread</h3> <p>Because the state is just a tiny object in memory (~KB), much lighter than the OS thread. And doing “mount”/”unmount”, CPU does not perform “context switching”, so we can avoid the cost I mention in previous article.</p> <p>So what I mean when I said CPU doesn’t perform “context switching”. The work Carrier Thread do is mounting/unmounting between Virtual Threads. We can assume that the Carrier Thread now acts as the physical CPU core. The “context switching” becomes mounting/unmounting with very low memory cost.</p> <p>The cost for CPU “context switching” is now approximately zero. Because the OS threads are always have work to do.</p> <p>The question is, no matter whether it’s an OS thread or a Virtual Thread, the Blocking I/O is still there. Because the Virtual Threads are just higher level tasks run on OS threads as any other tasks, if it’s blocked by I/O, OS thread is still be blocked, especially we still use the <code class="language-plaintext highlighter-rouge">java.io</code>, right?</p> <p>Don’t worry, the JVM has been rewritten to check every when we call a standard Java I/O method (“atomic” task): If this is a Virtual Thread, unmount it.</p> <h2 id="the-pinning-problem">The Pinning Problem</h2> <p><strong>Pinning</strong> happens when a Virtual Thread gets stuck to its Carrier Thread.</p> <p>The reason is:</p> <ul> <li>Executing inside a <code class="language-plaintext highlighter-rouge">synchronized</code> block.</li> <li>Call native method (JNI), e.g. C/C++, assembly.</li> </ul> <h2 id="async-and-virtual-thread-which-one-is-better">Async and Virtual Thread, which one is better?</h2> <p>We have some reason when we should use each of them.</p> <h3 id="virtual-thread">Virtual Thread</h3> <ul> <li><strong>Readability</strong>: We can write “linear” code, the old-school style that all people have already written.</li> <li><strong>Stack traces</strong>: If an exception thrown in Async, the stack trace often points to the internal async runner rather than our logic. Pollute the stack trace lead to harder to debug.</li> <li><strong>Compatibility</strong>: We can use the existing libraries even if not designed for async. We mostly don’t need to change the old code.</li> </ul> <h3 id="async">Async</h3> <ul> <li><strong>Memory control</strong>: In case we want to build ultra high performance system, async allows us to completely control when the memory is allocated and when tasks move.</li> <li><strong>Java &lt;21</strong>: If we must use any Java version before 21, we don’t have the Virtual Thread so the only choice is async.</li> <li><strong>Pinning problem</strong></li> </ul> <h2 id="conclusion">Conclusion</h2> <p>For most business applications, Virtual Thread is better. It makes coding easier, reduce code complexity. Less effort to join the project.</p> <p>If you have any question or suggestions, feel free to ask or share your opinion in the comment section.</p>]]></content><author><name></name></author><category term="software"/><category term="java"/><category term="concurrency"/><category term="system-design"/><category term="performance"/><category term="virtual-threads"/><summary type="html"><![CDATA[How Java Virtual Threads work under the hood, why they simplify concurrency, and when I choose them over async code.]]></summary></entry><entry><title type="html">Why Blocking I/O Hurts and How Asynchronous Fixes It</title><link href="https://www.dinhphu28.com/blog/2026/why-blocking-io-hurts-and-how-asynchronous-fixes-it/" rel="alternate" type="text/html" title="Why Blocking I/O Hurts and How Asynchronous Fixes It"/><published>2026-04-17T06:52:00+00:00</published><updated>2026-04-17T06:52:00+00:00</updated><id>https://www.dinhphu28.com/blog/2026/why-blocking-io-hurts-and-how-asynchronous-fixes-it</id><content type="html" xml:base="https://www.dinhphu28.com/blog/2026/why-blocking-io-hurts-and-how-asynchronous-fixes-it/"><![CDATA[<h2 id="problem-of-blocking-io-way">Problem of Blocking I/O Way</h2> <p>We know that, not all process in our code is CPU-bound. Some of them are I/O-bound, such as network calls, file system access, database queries, etc.</p> <p>Almost software applications process a lot of I/O-bound tasks. But if and thread is blocked while waiting for an I/O operation to complete, it cannot do anything else. Result in inefficient resource utilization and poor performance.</p> <p>The question is, why we don’t create a new thread for each I/O-bound task? The answer is, creating and managing threads can be expensive in terms of system resources. If we create too many threads, it can lead to thread contention, increased memory usage, and even system instability.</p> <p>E.g: Java OS thread stack size is 1MB by default. If we create 1000 threads, it will consume 1GB of memory just for the thread stacks. Okay, we can pay more memory cost to create more threads, but in reality, it almost doesn’t resolve the problem.</p> <p>And I think with the current prices of RAM, we don’t want to do that.</p> <p>Why it takes so much cost? - “Context switching”.</p> <p>We know that CPU will read from L1, L2 cache (much faster than RAM) before reading from main memory. When a thread is switched out, the CPU needs to save the current state of the thread (including registers, program counter, etc.) and load the state of the next thread to be executed. This process is called context switching.</p> <p>If we always do “context switching”, the cache efficiency will lost. When a thread is switched out, the CPU’s cache may be filled with data that is relevant to the thread that is being switched out. When the next thread is switched in, it may need to access different data that is not in the cache, leading to cache misses and increased latency. Now the CPU will do useless work than doing the actual task.</p> <h2 id="asynchronous">Asynchronous</h2> <p>Because we cannot create a new thread for each I/O-bound task, we need to find a way to allow the thread to do other work while waiting for the I/O operation to complete. This is where asynchronous programming comes in.</p> <p>The async concept is decoupling when and what to execute.</p> <p>How does it work?</p> <p>Think at the lowest level task which just do I/O operation, I call it “atomic task”. Each higher level task is composed of multiple atomic tasks, called “task chain”. When an atomic task is waiting for I/O operation to complete, it can yield control to the next atomic task.</p> <pre><code class="language-pseudocode">CLASS Task
    PROPERTY next

    METHOD run()
        execute((result) -&gt; {
            IF result.success AND next != null THEN
                next.run()
            ELSE IF NOT result.success THEN
                HandleError()
            END IF
        })
END CLASS


// usage
taskA.next &lt;- taskB
taskB.next &lt;- taskC

taskA.run()
</code></pre> <p>Ideally, we have no blocking thread, all of them always be processed by the CPU, so the number of threads should be equal to the number of CPU cores. I will show you this problem and why in Java people usually use more threads than CPU cores right after this.</p> <p>Some folks may ask, finally, the atomic task still needs to wait for the I/O, it still blocks the thread, right?</p> <p>First, instead let the CPU wait, it hands the job of to the OS Kernel using features like epoll, IOCP and the hardware controller will notify the OS -&gt; application when the I/O operation is complete to continue the tasks.</p> <p>But some cases, such as JDBC drivers, do not support async operations, so the thread will be blocked while waiting for the I/O operation to complete.</p> <p>Right, we can think about the R2DBC, but forget it, it’s just a special case about the database drivers.</p> <p>Yeah, we realize that, if we have many JDBC tasks, we have no more thread to do other tasks. So we need create more threads to CPU pause current thread and swap in another one, which continue the task chain. When the JDBC’s thread is completed, the CPU will swap back the thread to continue.</p> <p>With this approach, we can minimize the context switching but still do tasks efficiently.</p> <h2 id="the-backpressure-problem">The backpressure problem</h2> <p>Because in async, we can have many tasks running in fraction of second. We will accidentally DDoS our database, right?</p> <p>No, the thread pool which has a limited number of threads will act as a safety valve to limit number of threads can query the database.</p> <p>But if we let too many threads in thread pool, the DB will crash. Another approach is database connection pool, such as HikariCP. Especially when we use Virtual Thread that allow us to create almost unlimited threads.</p> <h2 id="downside">Downside</h2> <p>For developer, we must manually split the code into pieces. We must write the “start” part, then provide a callback for the “finish” part. The thread is freed because we explicitly ended the function. So the code is harder to read, debug. I think everyone knows “callback hell”. Anyway, we have other syntax makes it easier like async/await in JavaScript, for/yield in Scala. But I think the “linear” coding style like synchronous is easier to swallow.</p> <p>In next article, I will show you how the Virtual Thread works and why we should use it.</p>]]></content><author><name></name></author><category term="software"/><category term="system-design"/><category term="concurrency"/><category term="performance"/><category term="async"/><category term="io"/><category term="java"/><summary type="html"><![CDATA[Blocking I/O wastes CPU cycles and limits scalability. This post explains how asynchronous programming solves the problem and improves performance.]]></summary></entry><entry><title type="html">Bloom Filter and How I Prevent Tons of Message Duplication</title><link href="https://www.dinhphu28.com/blog/2026/bloom-filter-and-application/" rel="alternate" type="text/html" title="Bloom Filter and How I Prevent Tons of Message Duplication"/><published>2026-04-16T10:57:00+00:00</published><updated>2026-04-16T10:57:00+00:00</updated><id>https://www.dinhphu28.com/blog/2026/bloom-filter-and-application</id><content type="html" xml:base="https://www.dinhphu28.com/blog/2026/bloom-filter-and-application/"><![CDATA[<h2 id="problem">Problem</h2> <p>Our Customer Experience Platform processes tens of millions of messages per day, and we need to prevent sending duplicate messages to end users within a recent time window.</p> <p>There are many ways to do this, such as:</p> <ul> <li>Database lookup</li> <li>Saving and looking up message keys in Redis</li> </ul> <p>The first option is a bad idea. With hundreds of millions of messages sent each week, every message would require a lookup to check whether it has already been sent. This takes a lot of time and puts high pressure on the database, even with proper indexing.</p> <p>The Redis approach is quite good if the dataset is small. However, it consumes a lot of memory. With limited RAM, this solution is not ideal.</p> <p>At that time, I thought we must have something better.</p> <p>That is <strong>Bloom Filter</strong> — just a data structure, but it can do this job efficiently with only MB-level memory.</p> <p>The idea is before sending each message, we check whether it has already been sent. If not, we send it and then add it to the Bloom Filter.</p> <p>The downside is that there can be false positives.</p> <h2 id="what-is-bloom-filter">What is Bloom Filter</h2> <p>A Bloom Filter is a space-efficient probabilistic data structure used to check whether an element is a member of a set.</p> <p>Why is it called <em>probabilistic</em>? Because there can be false positive results.</p> <p>In my case, the system checks whether the same message has been sent before. If the result is false, we can be 100% sure that it has never been sent, so we can safely send it without any database or Redis lookup.</p> <p>However, if the result is true, we cannot be completely sure that it has been sent. This is what we call a false positive.</p> <p>A Bloom Filter is a fixed-size bit array. We denote its size as <strong>m</strong>.</p> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/bloom-filter-1.svg-480.webp 480w,/assets/img/bloom-filter-1.svg-800.webp 800w,/assets/img/bloom-filter-1.svg-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/bloom-filter-1.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="quick lookup" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Initialize Bloom Filter with size m=10 </div> <p>We need <strong>k</strong> independent hash functions to compute hash values for each input.</p> <p>When an element is added to the filter, we hash it using all <strong>k</strong> hash functions. Then we take modulo <strong>m</strong> to get indices in the bit array, and set those positions to 1.</p> <p>For example, suppose we want to add <code class="language-plaintext highlighter-rouge">"dinhphu28"</code> to a filter with size <strong>m</strong> = 10 (initialized with all zeros) and <strong>k</strong> = 3 hash functions:</p> \[\begin{aligned} h1(\texttt{"dinhphu28"}) \bmod 10 &amp;= 3 \\ h2(\texttt{"dinhphu28"}) \bmod 10 &amp;= 2 \\ h3(\texttt{"dinhphu28"}) \bmod 10 &amp;= 8 \end{aligned}\] <p>We set the bits at indices 3, 2, and 8 to 1.</p> <p>Now we have:</p> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/bloom-filter-2.svg-480.webp 480w,/assets/img/bloom-filter-2.svg-800.webp 800w,/assets/img/bloom-filter-2.svg-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/bloom-filter-2.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="quick lookup" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Add "dinhphu28" </div> <p>Now, add <code class="language-plaintext highlighter-rouge">"jack"</code>:</p> \[\begin{aligned} h1(\texttt{"jack"}) \bmod 10 &amp;= 1 \\ h2(\texttt{"jack"}) \bmod 10 &amp;= 5 \\ h3(\texttt{"jack"}) \bmod 10 &amp;= 8 \end{aligned}\] <p>We set bits at indices 1, 5, and 8 to 1:</p> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/bloom-filter-3.svg-480.webp 480w,/assets/img/bloom-filter-3.svg-800.webp 800w,/assets/img/bloom-filter-3.svg-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/bloom-filter-3.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="quick lookup" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Add "jack" </div> <p>To check whether <code class="language-plaintext highlighter-rouge">"dinhphu28"</code> exists in the filter, we compute the indices again. Instead of writing, we check whether all corresponding bits are set to 1.</p> <ul> <li>If all bits are 1 → the element <em>probably</em> exists</li> <li>If any bit is 0 → the element definitely does not exist</li> </ul> <p>So why do I say <strong>probably</strong>? Let’s look at this example.</p> <p>Check <code class="language-plaintext highlighter-rouge">"adam"</code>:</p> \[\begin{aligned} h1(\texttt{"adam"}) \bmod 10 &amp;= 5 \\ h2(\texttt{"adam"}) \bmod 10 &amp;= 1 \\ h3(\texttt{"adam"}) \bmod 10 &amp;= 2 \end{aligned}\] <p>All indices (5, 1, 2) are already set, even though we never added <code class="language-plaintext highlighter-rouge">"adam"</code> to the filter.</p> <div class="row"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/bloom-filter-4.svg-480.webp 480w,/assets/img/bloom-filter-4.svg-800.webp 800w,/assets/img/bloom-filter-4.svg-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/bloom-filter-4.svg" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="quick lookup" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Check "adam" </div> <p>Because these bits were set by other elements, the filter returns that <code class="language-plaintext highlighter-rouge">"adam"</code> probably exists — but it does not. This is a false positive.</p> <h2 id="how-i-resolve-the-duplication-problem">How I Resolve the Duplication Problem</h2> <p>Each message has a unique key. In my case, it is a combination of the message template ID and the recipient ID.</p> <p>After sending a message, I add this key to the Bloom Filter. Before sending a message, I check whether the key exists in the filter.</p> <ul> <li>If the result is false → I am sure the message has never been sent → send it</li> <li>If the result is true → it might be a false positive → I drop it</li> </ul> <p>I choose to drop messages in case of uncertainty because the cost of sending duplicate messages is much higher than the cost of dropping a few valid ones.</p> <p>For dropped messages, I push them to Kafka for offline processing to rebuild the dataset and handle them later.</p> <p>With this approach, I can prevent tons of duplicate messages while using only MBs of memory for the Bloom Filter.</p> <p>Some people might ask: what about multiple service instances? How do we share the Bloom Filter?</p> <p>The answer is <strong>Redis Bloom</strong>, a Redis module that provides Bloom Filter data structures.</p> <p>With Redis Bloom, we can share the filter across multiple service instances.</p> <pre><code class="language-mermaid">---
config:
    theme: redux-color
    look: handDrawn
    markdownAutoWrap: false
---

flowchart TD
    A["Event Incoming"] --&gt; B{"Redis Bloom"}

    B -- Hit --&gt; F["Drop Event&lt;br/&gt;(Duplicate)"]
    B -- Miss --&gt; G["Process Event"]

    G --&gt; H["Persist Event Status"]
    H --&gt; I["Add to Redis Bloom"]

    I --&gt; K["End"]
    F --&gt; L

    %% Optional offline recovery
    L["Offline Rebuild Dataset&lt;br/&gt;(Handle False Positives)"]
    L --&gt; K
</code></pre>]]></content><author><name></name></author><category term="software"/><category term="system-design"/><category term="data-structure"/><summary type="html"><![CDATA[Bloom Filter is a probabilistic data structure with low memory cost, used to test whether an object is a member of a set.]]></summary></entry><entry><title type="html">Choosing the Right Password Hashing Algorithm</title><link href="https://www.dinhphu28.com/blog/2026/choosing-the-right-password-hashing-algorithm/" rel="alternate" type="text/html" title="Choosing the Right Password Hashing Algorithm"/><published>2026-03-25T07:30:00+00:00</published><updated>2026-03-25T07:30:00+00:00</updated><id>https://www.dinhphu28.com/blog/2026/choosing-the-right-password-hashing-algorithm</id><content type="html" xml:base="https://www.dinhphu28.com/blog/2026/choosing-the-right-password-hashing-algorithm/"><![CDATA[<h2 id="problem-with-password-storage">Problem with Password Storage</h2> <p>When passwords are stored, they must remain secure even if the database is compromised.</p> <p>Even if your application has brute-force protection, what happens when an attacker gets the hashes? They can always brute-force them <strong>offline</strong>.</p> <p>With older hashing algorithms like MD5 or SHA-1, attackers can compute billions of hashes per second using modern hardware. That makes weak passwords trivial to crack.</p> <p>So, solution is: <strong>slow down offline attacks</strong> by using hashing algorithms that are intentionally expensive to compute.</p> <p>Below is a list of commonly recommended password hashing algorithms:</p> <ul> <li><strong>Argon2id</strong>: Winner of the 2015 Password Hashing Competition. Best choice for new systems. Strong resistance against both GPU-based attacks and side-channel attacks. Minimum recommended parameters: <code class="language-plaintext highlighter-rouge">memory = 19 MiB</code>, <code class="language-plaintext highlighter-rouge">iterations = 2</code>, <code class="language-plaintext highlighter-rouge">parallelism = 1</code></li> <li><strong>scrypt</strong>: Good alternative with strong memory hardness. Minimum configuration: <code class="language-plaintext highlighter-rouge">N = 2^17</code>, <code class="language-plaintext highlighter-rouge">r = 8</code>, <code class="language-plaintext highlighter-rouge">p = 1</code></li> <li><strong>bcrypt</strong>: Common in legacy systems. Still secure if configured properly. Recommended cost factor: <code class="language-plaintext highlighter-rouge">12+</code></li> <li><strong>PBKDF2</strong>: Mostly used when FIPS-140 compliance is required. Minimum configuration: <code class="language-plaintext highlighter-rouge">iterations = 600,000</code>, <code class="language-plaintext highlighter-rouge">hash = HMAC-SHA256</code></li> </ul> <p>Avoid fast hashing algorithms like MD5, SHA-1, or even SHA-256 for password hashing. They are designed for speed, which makes them easy to brute-force with modern hardware.</p> <h2 id="what-is-argon2id">What is Argon2id?</h2> <p>Argon2id is a variant of Argon2. It’s not just a password hashing algorithm — it’s a <strong>key derivation function (KDF)</strong>.</p> <h3 id="what-is-a-key-derivation-function-kdf">What is a Key Derivation Function (KDF)?</h3> <p>A KDF is used to derive secure cryptographic keys from a secret input (like a password).</p> <p>Purpose:</p> <ul> <li>Make brute-force attacks slower</li> <li>Increase the cost of guessing each password</li> </ul> <p>In practice, a KDF turns a weak input (human password) into something much harder to attack.</p> <h2 id="why-argon2id">Why Argon2id?</h2> <p>Argon2 has three variants:</p> <ul> <li><strong>Argon2d</strong> Optimized for resistance against GPU attacks (memory-hard), but vulnerable to side-channel attacks.</li> <li><strong>Argon2i</strong> Resistant to side-channel attacks, but weaker against GPU-based brute-force.</li> <li><strong>Argon2id</strong> Hybrid approach — combines both. This makes it the most balanced and recommended option.</li> </ul> <p>Besides the security benefits, there are trade-offs:</p> <ul> <li>More resource-intensive (CPU + memory)</li> <li>Slightly more complex to implement correctly</li> </ul> <h2 id="conclusion">Conclusion</h2> <p>If you’re building a new system, use <strong>Argon2id</strong> with recommended parameters.</p> <p>For existing systems, <strong>bcrypt</strong> is still a safe and practical choice.</p> <p>Avoid fast hashing algorithms like MD5 or SHA-based hashes for passwords.</p> <p>The goal is not just to hash passwords, but to make brute-force attacks as expensive as possible.</p>]]></content><author><name></name></author><category term="security"/><category term="cryptography"/><category term="security"/><category term="password-hashing"/><category term="best-practices"/><summary type="html"><![CDATA[A practical guide to secure password hashing, comparing Argon2id, bcrypt, scrypt, and PBKDF2 to prevent offline brute-force attacks.]]></summary></entry><entry><title type="html">Error Handling in Programming: Best Practices and Techniques</title><link href="https://www.dinhphu28.com/blog/2026/error-handling/" rel="alternate" type="text/html" title="Error Handling in Programming: Best Practices and Techniques"/><published>2026-02-05T09:30:00+00:00</published><updated>2026-02-05T09:30:00+00:00</updated><id>https://www.dinhphu28.com/blog/2026/error-handling</id><content type="html" xml:base="https://www.dinhphu28.com/blog/2026/error-handling/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>Error handling is one of those topics every developer uses daily, but rarely stops to think about <em>systematically</em>. Different languages promote very different styles, and those styles deeply influence how we design systems.</p> <p>In this post, I want to step back and answer three questions:</p> <ul> <li>What are the fundamental models of error handling?</li> <li>How do those models affect system architecture?</li> <li>What are practical patterns you can apply when writing real services?</li> </ul> <p>This is not a language-specific tutorial. Instead, it’s about <em>how to think</em> about errors.</p> <h2 id="what-is-error-handling">What Is Error Handling?</h2> <p>At a high level, <em>error handling is how software reacts to unexpected conditions</em>:</p> <ul> <li>invalid user input</li> <li>I/O failures</li> <li>unavailable resources</li> <li>violated assumptions</li> </ul> <p>Some people think errors are bugs, but:</p> <ul> <li>Errors are expected events in an imperfect world.</li> <li>Bugs are mistakes in the code.</li> </ul> <p>Good error handling makes failures:</p> <ul> <li>explicit</li> <li>observable</li> <li>diagnosable</li> <li>recoverable (when appropriate)</li> </ul> <h2 id="four-fundamental-error-handling-models">Four Fundamental Error-Handling Models</h2> <p>Across programming languages, most error-handling mechanisms fall into 4 core models.</p> <h3 id="1-exception-based-error-handling">1. Exception-based Error Handling</h3> <p>Errors are <em>control flow</em>. They interrupt normal execution and jump up the call stack.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">try</span> <span class="o">{</span>
    <span class="n">articleService</span><span class="o">.</span><span class="na">findById</span><span class="o">(</span><span class="n">id</span><span class="o">);</span>
<span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">ArticleNotFound</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
    <span class="c1">// Handle ArticleNotFound here</span>
<span class="o">}</span>
</code></pre></div></div> <p>Characteristics:</p> <ul> <li>Non-local control flow</li> <li>Stack unwinding</li> <li>Centralized handlers possible</li> </ul> <p>Typical languages</p> <ul> <li>Java, C#, Scala</li> <li>Python, JavaScript, Ruby</li> </ul> <p>Strengths</p> <ul> <li>Clean happy-path code</li> <li>Easy global handling</li> </ul> <p>Weaknesses</p> <ul> <li>Hidden exits</li> <li>Harder reasoning in async / concurrent code</li> <li>Easy to over-catch or swallow errors</li> </ul> <p>This model works well when frameworks control the lifecycle (e.g. Spring, Quarkus, …).</p> <h3 id="2-error-as-values">2. Error-as-Values</h3> <p>Errors are returned explicitly as values, alongside results.</p> <div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">article</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">service</span><span class="o">.</span><span class="n">Find</span><span class="p">(</span><span class="n">id</span><span class="p">)</span>
<span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
    <span class="k">return</span> <span class="n">err</span>
<span class="p">}</span>
</code></pre></div></div> <p>Characteristics</p> <ul> <li>Explicit, linear control flow</li> <li>No hidden stack jumps</li> <li>Caller decides how to handle</li> </ul> <p>Typical languages</p> <ul> <li>Go</li> <li>C (return codes)</li> <li>Zig (partially)</li> </ul> <p>Strengths</p> <ul> <li>Easy to reason about</li> <li>Predictable behavior</li> <li>Excellent for concurrent systems</li> </ul> <p>Weaknesses</p> <ul> <li>Verbose</li> <li>No compile-time enforcement</li> </ul> <p>This model is very common in system software and distributed services.</p> <h3 id="3-result--algebraic-error-types">3. Result / Algebraic Error Types</h3> <p>A function returns either a success value or a typed error.</p> <div class="language-rust highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">fn</span> <span class="nf">find</span><span class="p">(</span><span class="n">id</span><span class="p">:</span> <span class="n">Id</span><span class="p">)</span> <span class="k">-&gt;</span> <span class="nb">Result</span><span class="o">&lt;</span><span class="n">Article</span><span class="p">,</span> <span class="n">FindError</span><span class="o">&gt;</span>
</code></pre></div></div> <div class="language-scala highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">findArticleById</span><span class="o">(</span><span class="n">id</span><span class="k">:</span> <span class="kt">String</span><span class="o">)</span><span class="k">:</span> <span class="kt">Either</span><span class="o">[</span><span class="kt">ArticleNotFoundException</span>, <span class="kt">Article</span><span class="o">]</span>
</code></pre></div></div> <p>Characteristics</p> <ul> <li>Explicit like error values</li> <li>Errors are <em>typed</em></li> <li>Compiler forces handling</li> </ul> <p>Typical languages</p> <ul> <li>Rust</li> <li>Scala (Either, Try)</li> <li>Haskell</li> <li>Swift</li> </ul> <p>Strengths</p> <ul> <li>Very strong correctness guarantees</li> <li>Excellent for domain modeling</li> <li>Great for libraries</li> </ul> <p>Weaknesses</p> <ul> <li>Type noise</li> <li>Can “infect” APIs</li> </ul> <p>You can think of this as checked exceptions done right.</p> <h3 id="4-effect-based-error-handling">4. Effect-based Error Handling</h3> <p>Errors are modeled as <em>effects</em> in the type system, handled separately from logic.</p> <pre><code class="language-pseudocode">findArticle : Id -&gt; Article ! { ArticleError }
</code></pre> <p>Typical languages/frameworks</p> <ul> <li>OCaml (algebraic effects)</li> <li>Koka</li> <li>Scala ZIO</li> <li>TypeScript (Effect library)</li> </ul> <p>Strengths</p> <ul> <li>Extreme composability</li> <li>Minimal boilerplate</li> <li>Very powerful abstractions</li> </ul> <p>Weaknesses</p> <ul> <li>Steep learning curve</li> <li>Overkill for most production systems</li> </ul> <p>This model is still mostly academic or research. Usually, I don’t use this model.</p> <h2 id="checked-vs-unchecked-vs-value-errors">Checked vs Unchecked vs Value Errors</h2> <p>This distinction mainly applies to exception-based systems.</p> <ul> <li>Checked exceptions (Java): The compiler forces you to catch or declare them. Usually used by library writer.</li> <li>Unchecked exceptions: No compiler enforcement.</li> <li>Error values / Result types: Explicit by construction.</li> </ul> <p>Many modern languages moved away from checked exceptions because:</p> <ul> <li>They leak implementation details</li> <li>They force meaningless boilerplate</li> <li>They don’t scale well in large systems</li> </ul> <p>Instead, they prefer explicit return channels.</p> <h2 id="error-handling-as-an-architectural-decision">Error Handling as an Architectural Decision</h2> <p>Error handling is not just a language feature — it’s an <em>architectural choice</em>.</p> <table> <thead> <tr> <th>Architecture Type</th> <th>Suitable Model</th> </tr> </thead> <tbody> <tr> <td>Framework-centric</td> <td>Exceptions</td> </tr> <tr> <td>Microservices</td> <td>Error values</td> </tr> <tr> <td>Domain-driven design</td> <td>Result / Either</td> </tr> <tr> <td>System tools / daemons</td> <td>Error values</td> </tr> <tr> <td>Research / Language design</td> <td>Effects</td> </tr> </tbody> </table> <blockquote> <p>If failures are expected and frequent, make them explicit.</p> </blockquote> <h2 id="practical-error-handling-patterns">Practical Error Handling Patterns</h2> <h3 id="1-handle-errors-where-you-add-value">1. Handle Errors Where You Add Value</h3> <p>If you can’t improve the error, don’t handle it.</p> <p>Bad:</p> <div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
    <span class="n">log</span><span class="o">.</span><span class="n">Println</span><span class="p">(</span><span class="n">err</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">err</span>
<span class="p">}</span>
</code></pre></div></div> <p>Good:</p> <div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="n">err</span> <span class="o">!=</span> <span class="no">nil</span> <span class="p">{</span>
    <span class="k">return</span> <span class="n">fmt</span><span class="o">.</span><span class="n">Errorf</span><span class="p">(</span><span class="s">"find article %s: %w"</span><span class="p">,</span> <span class="n">id</span><span class="p">,</span> <span class="n">err</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div> <h3 id="2-translate-errors-at-boundaries">2. Translate Errors at Boundaries</h3> <p>Lower layers deal with technical errors. Higher layers deal with business meaning.</p> <div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="n">errors</span><span class="o">.</span><span class="n">Is</span><span class="p">(</span><span class="n">err</span><span class="p">,</span> <span class="n">sql</span><span class="o">.</span><span class="n">ErrNoRows</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">return</span> <span class="n">ArticleNotFound</span>
<span class="p">}</span>
</code></pre></div></div> <p>This keeps domain logic clean and decoupled from infrastructure.</p> <h3 id="3-log-errors-only-once">3. Log Errors Only Once</h3> <p>If you return an error, don’t log it. If you log an error, don’t return it.</p> <p>Log at system boundaries:</p> <ul> <li>HTTP handlers</li> <li>CLI entry points</li> <li>goroutine roots</li> <li>message consumers</li> </ul> <p>This avoids duplicated logs and preserves context.</p> <h3 id="4-panic-is-not-error-handling">4. Panic Is Not Error Handling</h3> <p>In Go and Rust, <code class="language-plaintext highlighter-rouge">panic</code> is not an exception system.</p> <p>Use panic only for:</p> <ul> <li>programmer bugs</li> <li>violated invariants</li> <li>impossible states</li> </ul> <p>Expected failures should <em>never</em> panic.</p> <h2 id="testing-error-paths">Testing Error Paths</h2> <p>Good error handling is testable.</p> <ul> <li>Unit tests should assert error types</li> <li>Integration tests should assert error mapping</li> <li>Observability (logs, metrics) should expose failure modes</li> </ul> <p>Errors are part of your API, so test them like features.</p> <h2 id="my-practial-recommendation">My Practial Recommendation</h2> <p>Do:</p> <ul> <li>Fail fast, handle later.</li> <li>Use specific error type for specific business.</li> <li>Meaningful error messages.</li> <li>Wrap and re-throw when adding context.</li> <li>Standardize error responses (e.g. RFC-9457 ProblemDetails).</li> <li>Document the error (e.g. Javadoc).</li> <li>Catch the most specific error first.</li> </ul> <p>Don’t:</p> <ul> <li>Log too much but hard to understand.</li> <li>Swallow error.</li> <li>Catch all as the same (e.g. Throwable).</li> </ul> <h2 id="conclusion">Conclusion</h2> <p>In this article, I have shown you an overview about the error handling and best practices.</p> <ul> <li>There are four fundamental error-handling models</li> <li>Each model fits different system architectures</li> <li>Explicit error handling scales better in distributed systems</li> <li>Log once, at boundaries</li> <li>Treat errors as first-class design elements</li> </ul> <p>Good error handling doesn’t eliminate failures, it makes failures <em>boring</em>, <em>visible</em>, and <em>understandable</em>.</p> <p>Feel free to leave your comments if you have any suggestion or question!</p>]]></content><author><name></name></author><category term="software"/><category term="programming"/><category term="error-handling"/><category term="best-practices"/><summary type="html"><![CDATA[Error handling is one of those topics every developer uses daily, but rarely stops to think about systematically. This post will show you an overview about error handling and best practice.]]></summary></entry><entry><title type="html">How to set up SSH Certificate Authentication with Your Own CA</title><link href="https://www.dinhphu28.com/blog/2025/how-to-set-up-ssh-certificate-authentication-with-your-own-ca/" rel="alternate" type="text/html" title="How to set up SSH Certificate Authentication with Your Own CA"/><published>2025-09-17T06:00:00+00:00</published><updated>2025-09-17T06:00:00+00:00</updated><id>https://www.dinhphu28.com/blog/2025/how-to-set-up-ssh-certificate-authentication-with-your-own-ca</id><content type="html" xml:base="https://www.dinhphu28.com/blog/2025/how-to-set-up-ssh-certificate-authentication-with-your-own-ca/"><![CDATA[<p><strong>TL;DR</strong>: SSH certificates replace traditional key management with a CA-based model. Hosts and users get signed certificates instead of raw keys, making authentication more secure, scalable, and easier to manage. This guide shows you how to set up your own SSH CA for both host and user authentication.</p> <h2 id="introduction">Introduction</h2> <p>Managing SSH keys at scale is difficult:</p> <ul> <li>Password authentication is insecure.</li> <li>TOFU (Trust On First Use) introduces risks.</li> <li>Distributing and updating keys across servers is a hassle.</li> <li>Offboarding requires manual cleanup on every host.</li> </ul> <p>SSH certificate authentication solves these problems by using a <strong>Certificate Authority (CA)</strong> to sign and verify both host and user keys.</p> <h2 id="benefits">Benefits</h2> <ul> <li>Centralized management of keys</li> <li>Simplified distribution and revocation</li> <li>Stronger authentication with signatures</li> <li>Easier onboarding and offboarding</li> </ul> <h2 id="how-it-works">How It Works</h2> <ul> <li><strong>Host CA</strong> signs server keys</li> <li><strong>User CA</strong> signs client keys</li> <li>Server trusts the User CA to verify users</li> <li>Client trusts the Host CA to verify servers</li> </ul> <p>Connection flow:</p> <ol> <li>Server presents signed host certificate → client verifies against Host CA</li> <li>Client presents signed user certificate → server verifies against User CA</li> </ol> <h2 id="setup">Setup</h2> <h3 id="1-generate-ca-keys">1. Generate CA keys</h3> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh-keygen <span class="nt">-t</span> ed25519 <span class="nt">-f</span> user_ca_key <span class="nt">-C</span> dinhphu28_user_ca
ssh-keygen <span class="nt">-t</span> ed25519 <span class="nt">-f</span> host_ca_key <span class="nt">-C</span> dinhphu28_host_ca
</code></pre></div></div> <p>Keep the private keys secure.</p> <h3 id="2-host-certificate">2. Host certificate</h3> <p>On the server:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh-keygen <span class="nt">-f</span> ssh_host_ed25519_key <span class="nt">-N</span> <span class="s1">''</span> <span class="nt">-t</span> ed25519
</code></pre></div></div> <p>Sign with Host CA:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh-keygen <span class="nt">-s</span> host_ca_key <span class="nt">-I</span> dinhphu28-mac-machine <span class="nt">-h</span> <span class="se">\</span>
  <span class="nt">-n</span> <span class="s2">"example-host.dinhphu28.com,10.8.0.6"</span> <span class="nt">-V</span> +365d ssh_host_ed25519_key.pub
</code></pre></div></div> <h3 id="3-configure-server">3. Configure server</h3> <p><code class="language-plaintext highlighter-rouge">/etc/ssh/sshd_config</code>:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>HostKey /usr/etc/ssh/ssh_host_ed25519_key
HostCertificate /usr/etc/ssh/ssh_host_ed25519_key-cert.pub
TrustedUserCAKeys /usr/etc/ssh/user_ca_key.pub
</code></pre></div></div> <p>Restart sshd:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>systemctl restart sshd
</code></pre></div></div> <h3 id="4-user-certificate">4. User certificate</h3> <p>On the client:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh-keygen <span class="nt">-t</span> ed25519 <span class="nt">-C</span> <span class="s2">"robert@dinhphu28.com"</span>
</code></pre></div></div> <p>Sign with User CA:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh-keygen <span class="nt">-s</span> user_ca_key <span class="nt">-I</span> <span class="s2">"robert@dinhphu28.com"</span> <span class="nt">-n</span> <span class="s2">"robert"</span> <span class="nt">-V</span> +1d id_ed25519.pub
</code></pre></div></div> <h3 id="5-configure-client">5. Configure client</h3> <p>Move cert to <code class="language-plaintext highlighter-rouge">~/.ssh/</code>. Add Host CA to <code class="language-plaintext highlighter-rouge">~/.ssh/known_hosts</code>:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>@cert-authority *.dinhphu28.com,10.8.0.6 ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIFraYAO41YSLterrzte7TknguJWpTrCNC8MCL6xbcvbw
</code></pre></div></div> <h3 id="6-test">6. Test</h3> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh-keygen <span class="nt">-L</span> <span class="nt">-f</span> /path/to/your-cert.pub
ssh <span class="nt">-T</span> robert@example-host.dinhphu28.com
</code></pre></div></div> <h2 id="advanced">Advanced</h2> <p>Limitations of the basic setup:</p> <ul> <li>Revocation is manual</li> <li>Certificates have static expiration</li> </ul> <p>For larger environments, use automated solutions:</p> <ul> <li><a href="https://github.com/Netflix/bless">Netflix BLESS</a></li> <li><a href="https://developer.hashicorp.com/vault/docs/secrets/ssh">HashiCorp Vault SSH CA</a></li> </ul> <h2 id="conclusion">Conclusion</h2> <p>SSH certificate authentication provides scalable and secure management of users and hosts. With your own CA, you eliminate TOFU, simplify key distribution, and make onboarding/offboarding easier.</p>]]></content><author><name></name></author><category term="utilities"/><category term="ca"/><category term="openssh"/><category term="ssh"/><category term="security"/><category term="pki"/><summary type="html"><![CDATA[Learn how to set up SSH certificate authentication with your own CA to replace key distribution, improve security, and simplify management.]]></summary></entry><entry><title type="html">Build a Root and Intermediate Certificate Authority with Easy-RSA</title><link href="https://www.dinhphu28.com/blog/2025/build-a-root-and-intermediate-certificate-authority-with-easy-rsa/" rel="alternate" type="text/html" title="Build a Root and Intermediate Certificate Authority with Easy-RSA"/><published>2025-07-14T11:00:00+00:00</published><updated>2025-07-14T11:00:00+00:00</updated><id>https://www.dinhphu28.com/blog/2025/build-a-root-and-intermediate-certificate-authority-with-easy-rsa</id><content type="html" xml:base="https://www.dinhphu28.com/blog/2025/build-a-root-and-intermediate-certificate-authority-with-easy-rsa/"><![CDATA[<blockquote> <p>📖 This post continues from <a href="/blog/2025/why-build-your-own-certificate-authority/">Part 1: Why Build Your Own Certificate Authority</a></p> </blockquote> <h2 id="root-ca-setup">Root CA Setup</h2> <h3 id="1-download-and-extract-easy-rsa">1. Download and Extract Easy-RSA</h3> <p>Optionally rename the directory to <code class="language-plaintext highlighter-rouge">root-ca</code>.</p> <h3 id="2-create-or-edit-the-vars-file">2. Create or Edit the <code class="language-plaintext highlighter-rouge">vars</code> File</h3> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>set_var EASYRSA_REQ_COUNTRY    <span class="s2">"VN"</span>
set_var EASYRSA_REQ_PROVINCE   <span class="s2">"Ho Chi Minh City"</span>
set_var EASYRSA_REQ_CITY       <span class="s2">"Ho Chi Minh City"</span>
set_var EASYRSA_REQ_ORG        <span class="s2">"DINHPHU28 Root CA"</span>
set_var EASYRSA_REQ_EMAIL      <span class="s2">"ca@dinhphu28.com"</span>
set_var EASYRSA_REQ_OU         <span class="s2">"Community"</span>
set_var EASYRSA_ALGO           <span class="s2">"ec"</span>
set_var EASYRSA_DIGEST         <span class="s2">"sha512"</span>
set_var EASYRSA_CURVE          <span class="s2">"secp384r1"</span>
</code></pre></div></div> <h3 id="3-initialize-the-pki">3. Initialize the PKI</h3> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./easyrsa init-pki
</code></pre></div></div> <h3 id="4-build-the-root-ca">4. Build the Root CA</h3> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./easyrsa build-ca
</code></pre></div></div> <blockquote> <p>🔐 Remember the password — this secures your CA.</p> </blockquote> <p>You now have:</p> <ul> <li><code class="language-plaintext highlighter-rouge">pki/private/ca.key</code></li> <li><code class="language-plaintext highlighter-rouge">pki/ca.crt</code> (Root certificate)</li> </ul> <h3 id="5-create-custom-x509-type-for-intermediate-ca">5. Create Custom x509 Type for Intermediate CA</h3> <p>Create <code class="language-plaintext highlighter-rouge">x509-types/caRestricted</code>:</p> <div class="language-properties highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="py">basicConstraints</span> <span class="p">=</span> <span class="s">critical, CA:TRUE, pathlen:0</span>
<span class="py">subjectKeyIdentifier</span> <span class="p">=</span> <span class="s">hash</span>
<span class="py">authorityKeyIdentifier</span> <span class="p">=</span> <span class="s">keyid:always,issuer:always</span>
<span class="py">keyUsage</span> <span class="p">=</span> <span class="s">critical, cRLSign, keyCertSign</span>
</code></pre></div></div> <h2 id="intermediate-ca-setup">Intermediate CA Setup</h2> <p>Repeat steps 1–3 in a new directory like <code class="language-plaintext highlighter-rouge">intermediate-ca</code>, then update <code class="language-plaintext highlighter-rouge">vars</code>:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>set_var EASYRSA_REQ_ORG        <span class="s2">"DINHPHU28 EC CA"</span>
set_var EASYRSA_REQ_OU         <span class="s2">"Intermediate"</span>
</code></pre></div></div> <h3 id="1-initialize-and-create-request">1. Initialize and Create Request</h3> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./easyrsa init-pki
./easyrsa gen-req intermediate-ca
</code></pre></div></div> <p>Transfer <code class="language-plaintext highlighter-rouge">intermediate-ca.req</code> to the <strong>offline Root CA</strong>.</p> <h3 id="2-at-the-root-ca">2. At the Root CA</h3> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./easyrsa import-req /path/to/intermediate-ca.req intermediate-ca
<span class="nv">EASYRSA_NO_SAN</span><span class="o">=</span>1 ./easyrsa sign-req caRestricted intermediate-ca
</code></pre></div></div> <h3 id="3-back-at-intermediate-ca">3. Back at Intermediate CA</h3> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cp</span> /path/to/intermediate-ca.crt pki/ca.crt
<span class="nb">cp</span> /path/to/root-ca/pki/ca.crt pki/root-ca.crt
<span class="nb">cat </span>pki/ca.crt pki/root-ca.crt <span class="o">&gt;</span> pki/ca-chain.crt
</code></pre></div></div> <p>In case system warns missing files/directories, create them:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">touch </span>pki/serial
<span class="nb">mkdir </span>pki/certs_by_serial
</code></pre></div></div> <p>You now have a functional Intermediate CA.</p> <h2 id="issue-certificate-for-microsoft-365-authentication">Issue Certificate for Microsoft 365 Authentication</h2> <h3 id="1-create-openssl-config">1. Create OpenSSL Config</h3> <p>Save as <code class="language-plaintext highlighter-rouge">user_openssl.cnf</code>:</p> <div class="language-ini highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">[ req ]</span><span class="w">
</span><span class="py">default_bits</span><span class="w">       </span><span class="p">=</span><span class="w"> </span><span class="s">384</span>
<span class="py">prompt</span><span class="w">             </span><span class="p">=</span><span class="w"> </span><span class="s">no</span>
<span class="py">distinguished_name</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">dn</span>
<span class="py">req_extensions</span><span class="w">     </span><span class="p">=</span><span class="w"> </span><span class="s">req_ext</span>
<span class="py">string_mask</span><span class="w">        </span><span class="p">=</span><span class="w"> </span><span class="s">utf8only</span>
<span class="py">default_md</span><span class="w">         </span><span class="p">=</span><span class="w"> </span><span class="s">sha512</span>
<span class="w">
</span><span class="nn">[ dn ]</span><span class="w">
</span><span class="py">CN</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">Dinh Phu Nguyen</span>
<span class="w">
</span><span class="nn">[ req_ext ]</span><span class="w">
</span><span class="py">subjectAltName</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">@alt_names</span>
<span class="w">
</span><span class="nn">[ alt_names ]</span><span class="w">
</span><span class="py">otherName</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">1.3.6.1.4.1.311.20.2.3;UTF8:dinhphu@office.dinhphu28.com</span>
</code></pre></div></div> <h3 id="2-generate-key-and-csr">2. Generate Key and CSR</h3> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>openssl ecparam <span class="nt">-name</span> secp384r1 <span class="nt">-genkey</span> <span class="nt">-noout</span> <span class="nt">-out</span> dinhphu.key
openssl req <span class="nt">-new</span> <span class="nt">-key</span> dinhphu.key <span class="nt">-out</span> dinhphu.csr <span class="nt">-config</span> user_openssl.cnf
</code></pre></div></div> <h3 id="3-sign-the-csr-at-intermediate-ca">3. Sign the CSR at Intermediate CA</h3> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./easyrsa import-req /full/path/to/dinhphu.csr dinhphu
<span class="nv">EASYRSA_CERT_EXPIRE</span><span class="o">=</span>60 ./easyrsa <span class="nt">--subject-alt-name</span><span class="o">=</span><span class="s2">"otherName:1.3.6.1.4.1.311.20.2.3;UTF8:dinhphu@office.dinhphu28.com"</span> sign-req client dinhphu
</code></pre></div></div> <h3 id="4-verify-upn">4. Verify UPN</h3> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>openssl x509 <span class="nt">-in</span> pki/issued/dinhphu.crt <span class="nt">-noout</span> <span class="nt">-text</span> | <span class="nb">grep</span> <span class="nt">-A5</span> <span class="s2">"Subject Alternative Name"</span>
</code></pre></div></div> <p>Should contain your UPN like this:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>X509v3 Subject Alternative Name:
    otherName:1.3.6.1.4.1.311.20.2.3;UTF8:dinhphu@office.dinhphu28.com
</code></pre></div></div> <p><strong>Note:</strong> Above client cert <code class="language-plaintext highlighter-rouge">dinhphu.crt</code> is only contain client cert.</p> <p>To verify the client cert valid, we not only need the Root CA cert but also Intermediate CA (full chain of trust): <code class="language-plaintext highlighter-rouge">Root CA -&gt; Intermediate CA -&gt; Client cert</code></p> <p>Most of SSL Tools or System use use client cert with fullchain (intermediate-ca -&gt; leaf) to verify the client with only Root CA cert.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> /path/to/intermediate-ca/pki/ca.crt dinhphu.crt <span class="o">&gt;</span> dinhphu-fullchain.crt
</code></pre></div></div> <h2 id="create-pfx-certificate">Create PFX Certificate</h2> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>openssl pkcs12 <span class="nt">-export</span> <span class="se">\</span>
  <span class="nt">-inkey</span> dinhphu.key <span class="se">\</span>
  <span class="nt">-in</span> dinhphu.crt <span class="se">\</span>
  <span class="nt">-certfile</span> /path-to/ca-chain.crt <span class="se">\</span>
  <span class="nt">-out</span> dinhphu.pfx <span class="se">\</span>
  <span class="nt">-name</span> <span class="s2">"Dinh Phu Nguyen"</span>
</code></pre></div></div> <blockquote> <p>You’ll be prompted for a password to protect the <code class="language-plaintext highlighter-rouge">.pfx</code>.</p> </blockquote> <h3 id="optional-macos-conversion">Optional: macOS Conversion</h3> <p>Because of this <code class="language-plaintext highlighter-rouge">pfx</code> using pkcs12 is not compatible with macOS, so we need to convert it to legacy.</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>openssl pkcs12 <span class="nt">-in</span> dinhphu.pfx <span class="nt">-out</span> dinhphu.pem
openssl pkcs12 <span class="nt">-export</span> <span class="nt">-in</span> dinhphu.pem <span class="nt">-out</span> dinhphu_macos.pfx <span class="nt">-legacy</span>
</code></pre></div></div> <h2 id="microsoft-entra-configuration-notes">Microsoft Entra Configuration Notes</h2> <h3 id="authentication-binding">Authentication Binding</h3> <table> <thead> <tr> <th>Certificate Attribute</th> <th>Value</th> </tr> </thead> <tbody> <tr> <td>Policy OID</td> <td><code class="language-plaintext highlighter-rouge">1.3.6.1.4.1.311.20.2.2</code></td> </tr> <tr> <td>Authentication strength</td> <td>Multi-factor</td> </tr> <tr> <td>Affinity binding</td> <td>Low</td> </tr> </tbody> </table> <p><strong>Add rule:</strong> Policy OID</p> <ul> <li>Certificate attribute: Policy OID</li> <li>Policy OID: <code class="language-plaintext highlighter-rouge">1.3.6.1.4.1.311.20.2.2</code></li> <li>Authentication strength: Mutli-factor authentication</li> <li>Affinity binding: Low</li> </ul> <h3 id="username-binding">Username Binding</h3> <table> <thead> <tr> <th>Certificate field</th> <th>Affinity binding</th> <th>User attribute</th> </tr> </thead> <tbody> <tr> <td>PrincipalName</td> <td>Low</td> <td>userPrincipalName</td> </tr> <tr> <td>RFC822Name</td> <td>Low</td> <td>userPrincipalName</td> </tr> </tbody> </table> <p><a href="https://learn.microsoft.com/en-us/entra/identity/authentication/how-to-certificate-based-authentication#step-4-configure-username-binding-policy">Microsoft Docs – Certificate-Based Authentication</a></p> <h2 id="wrapping-up">Wrapping Up</h2> <p>You’ve now built a secure, scalable two-tier Certificate Authority using Easy-RSA:</p> <ul> <li>A Root CA that remains offline for maximum trust</li> <li>An Intermediate CA that handles day-to-day certificate issuance</li> <li>End-entity certificates for services like Microsoft 365 with UPN-based binding</li> <li>Exportable PFX files and compatibility with various systems including macOS</li> </ul> <p>This setup not only strengthens your internal security posture but also gives you complete control over your certificate lifecycle, policies, and trust relationships.</p> <h2 id="whats-next">What’s Next?</h2> <p>In the upcoming article, I’ll share how I manage <strong>SSH access for a large number of users and servers</strong>, using a certificate-based and policy-driven approach to simplify operations and tighten security.</p> <p>If you’re building out a lab, managing production systems, or exploring PKI for your org — you’re on the right path. Feel free to leave questions or comments below.</p> <p>Happy cert-ing!</p>]]></content><author><name></name></author><category term="utilities"/><category term="ssl"/><category term="ca"/><category term="easy-rsa"/><category term="openssl"/><category term="security"/><category term="tutorial"/><summary type="html"><![CDATA[Step-by-step guide to building a Root CA and Intermediate CA using Easy-RSA, including issuing certificates for Microsoft 365 authentication.]]></summary></entry><entry><title type="html">Why Build Your Own Certificate Authority</title><link href="https://www.dinhphu28.com/blog/2025/why-build-your-own-certificate-authority/" rel="alternate" type="text/html" title="Why Build Your Own Certificate Authority"/><published>2025-07-14T07:30:00+00:00</published><updated>2025-07-14T07:30:00+00:00</updated><id>https://www.dinhphu28.com/blog/2025/why-build-your-own-certificate-authority</id><content type="html" xml:base="https://www.dinhphu28.com/blog/2025/why-build-your-own-certificate-authority/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>Managing a large number of users or devices in an organization - or even a home lab - introduces several challenges:</p> <ul> <li>Data security and privacy</li> <li>Establishing trust between systems</li> <li>Ensuring integrity of data and systems</li> <li>Automating access and lifecycle management</li> <li>Providing phishing-resistant authentication</li> </ul> <h2 id="use-cases-for-certificate-based-authentication">Use Cases for Certificate-Based Authentication</h2> <p>CA (Certificate Authority) can be used for securing many kinds of infrastructure, such as:</p> <ul> <li>Securing web servers (HTTPS)</li> <li>VPN authentication</li> <li>Email encryption (S/MIME)</li> <li>IoT device identity</li> <li>Cloud services and API trust</li> <li>Mutual TLS (mTLS) authentication</li> </ul> <h2 id="self-signed-vs-public-cas">Self-Signed vs. Public CAs</h2> <p>When evaluating certificate options, you may consider self-signed certificates or public Certificate Authorities (CAs). However:</p> <h3 id="self-signed-certificates">Self-Signed Certificates</h3> <ul> <li>Not trusted by browsers or operating systems by default</li> <li>Difficult to distribute and manage in large environments</li> </ul> <h3 id="public-certificate-authorities">Public Certificate Authorities</h3> <ul> <li>Can be expensive for many certificates</li> <li>Offer limited control over certificate lifecycle or policy</li> </ul> <h2 id="the-better-alternative-build-your-own-ca">The Better Alternative: Build Your Own CA</h2> <p>By building your own Certificate Authority using tools like <strong>Easy-RSA</strong>, you gain:</p> <ul> <li>Complete control over your PKI</li> <li>Zero cost per certificate</li> <li>Ability to define your own trust policies and usage</li> </ul> <p>Easy-RSA is a simple yet powerful command-line utility to manage your own Public Key Infrastructure (PKI).</p> <p>This gives you <strong>total control</strong>, <strong>zero recurring cost</strong>, and the ability to build a trust model that suits your environment.</p> <h2 id="two-tier-ca-hierarchy">Two-Tier CA Hierarchy</h2> <p>A best practice is to create:</p> <ul> <li><strong>Root CA</strong>: Offline, used only to sign Intermediate CA</li> <li><strong>Intermediate CA</strong>: Actively issues end-entity certificates</li> </ul> <blockquote> <p>🔐 Keep the Root CA offline to minimize security risks.</p> <p>Only use it when necessary to sign or revoke Intermediate CAs.</p> </blockquote> <pre><code class="language-mermaid">graph TD
    RootCA["🛡️ Root CA (Offline) - Signs Intermediate CA"]
    CRLRoot["📄 Root CRL (Exported)"]
    IntermediateCA["🔄 Intermediate CA (Online) - Signs End-Entity Certs"]
    CRLInt["📄 Intermediate CRL"]
    WebServer["🌐 Web Server"]
    EmailCert["📧 Email Client"]
    VPNCert["🔐 VPN Device"]

    RootCA --&gt;|Sign| IntermediateCA
    RootCA --&gt;|Publish| CRLRoot
    IntermediateCA --&gt;|Publish| CRLInt
    IntermediateCA --&gt;|Issue| WebServer
    IntermediateCA --&gt;|Issue| EmailCert
    IntermediateCA --&gt;|Issue| VPNCert
</code></pre> <h2 id="my-real-world-case">My Real-World Case</h2> <p>Let me share a bit of context. In my home lab, I manage:</p> <ul> <li>OpenVPN servers</li> <li>Microsoft 365 identity integrations</li> <li>Internal web applications</li> <li>Secure email with S/MIME</li> </ul> <p>Each time I added a new device or user, managing credentials was painful. Building a CA infrastructure simplified trust, reduced manual effort, and enabled automation.</p> <h2 id="whats-next">What’s Next?</h2> <p>In <a href="/blog/2025/build-a-root-and-intermediate-certificate-authority-with-easy-rsa/">Part 2</a>, I’ll walk you through the exact steps I used to build a Root CA, an Intermediate CA, and generate certificates with Easy-RSA.</p> <p>Stay tuned!</p>]]></content><author><name></name></author><category term="utilities"/><category term="ssl"/><category term="ca"/><category term="openssl"/><category term="easy-rsa"/><category term="security"/><category term="pki"/><summary type="html"><![CDATA[Explore the challenges of managing secure identities in your infrastructure, and why setting up your own Certificate Authority (CA) is the right solution.]]></summary></entry><entry><title type="html">ZRAM and how I deal with the memory usage in my Linux system</title><link href="https://www.dinhphu28.com/blog/2025/zram-swap/" rel="alternate" type="text/html" title="ZRAM and how I deal with the memory usage in my Linux system"/><published>2025-05-30T08:09:00+00:00</published><updated>2025-05-30T08:09:00+00:00</updated><id>https://www.dinhphu28.com/blog/2025/zram-swap</id><content type="html" xml:base="https://www.dinhphu28.com/blog/2025/zram-swap/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>I started using Linux in 2018 on a laptop with 4GB of RAM and a dual-core CPU.</p> <p>At first, I was happy with how my system performed. But over time, I noticed it becoming slower, especially as memory usage increased. Occasionally, the system would freeze for a few seconds, and I had to wait for it to respond again.</p> <p>I thought enabling swap might help improve performance. At the time, I didn’t know about ZRAM, so I simply enabled traditional disk-based swap.</p> <p>Thanks to the SSD, swap access was relatively fast—but still, the system didn’t feel as responsive as before, especially when I had multiple applications open.</p> <p>For a long time, I believed swap was the only solution to extend memory. Then I discovered <strong>ZRAM</strong>, and I began learning how it works and how to make use of it.</p> <h2 id="what-is-zram">What is ZRAM?</h2> <p><strong>ZRAM</strong> is a Linux kernel module that creates compressed, RAM-based block devices. These virtual devices can be used either as swap space or as general-purpose RAM disks.</p> <p>Yes, you read that right—<strong>swap space stored in RAM</strong> instead of on disk.</p> <p>At this point, you might be thinking: <em>“Isn’t swap supposed to expand available memory? Why would we use our already limited RAM for it?”</em></p> <p>That’s where ZRAM’s magic lies. By compressing data before storing it in RAM, ZRAM allows you to store more data in the same amount of memory. This makes your system feel like it has more RAM—without touching your disk.</p> <h2 id="how-does-zram-swap-work">How does ZRAM swap work?</h2> <p>Unlike traditional swap, which stores data on disk, <strong>ZRAM compresses and stores swap data directly in RAM</strong>.</p> <p>This means you can fit more data into your available memory. For example, with 4GB of physical RAM, ZRAM might effectively give you the equivalent of 6GB—depending on the compressibility of your data.</p> <p>Since RAM is significantly faster than any disk, this results in <strong>better system responsiveness</strong>, especially under memory pressure.</p> <p>Of course, compression and decompression use CPU resources. On low-end CPUs, this overhead might cause a performance hit. But on modern processors, it’s usually negligible—and the benefit of having “more RAM” tends to outweigh the cost.</p> <p>ZRAM supports several compression algorithms, such as <strong>LZO</strong>, <strong>LZ4</strong>, <strong>ZSTD</strong>, and others. Personally, I prefer <strong>ZSTD</strong> because it strikes a good balance between speed and compression ratio. It’s also the default in <strong>openSUSE Tumbleweed</strong>.</p> <h2 id="enabling-zram">Enabling ZRAM</h2> <p>For openSUSE Tumbleweed, enable ZRAM is easy. Just run the following command:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>zypper <span class="nb">install </span>systemd-zram-service <span class="o">&amp;&amp;</span> <span class="nb">sudo </span>zramswapon
</code></pre></div></div> <p>For Debian/Ubuntu, you can install the <code class="language-plaintext highlighter-rouge">zram-config</code> package:</p> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt <span class="nb">install </span>zram-config
<span class="nb">sudo </span>systemctl <span class="nb">enable </span>zram-config
<span class="nb">sudo </span>systemctl start zram-config
</code></pre></div></div> <p>Because of ZRAM swap (data compression), data should be pushed into ZRAM as soon as possible. Swappiness should be <code class="language-plaintext highlighter-rouge">180</code>.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>sysctl vm.swappiness<span class="o">=</span>180
</code></pre></div></div> <p>With <code class="language-plaintext highlighter-rouge">zstd</code>, <code class="language-plaintext highlighter-rouge">vm.page-cluster</code> should be <code class="language-plaintext highlighter-rouge">0</code> because of decompression.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>sysctl vm.page-cluster<span class="o">=</span>0
</code></pre></div></div> <h2 id="conclusion">Conclusion</h2> <p>I’ve been using <strong>ZRAM</strong> for a while now, and I can confidently say it has significantly improved my system’s performance. At the very least, it allows me to open dozens of browser tabs and run multiple applications without noticeable slowdowns.</p> <p>That said, ZRAM is <strong>not a replacement for physical RAM</strong>. Instead, it’s a smart way to make better use of the RAM you already have—especially under memory pressure.</p> <p>If your system has plenty of RAM, ZRAM might not be necessary. In fact, the CPU overhead for compression and decompression could slightly degrade performance in such cases.</p> <p>But if you’re working with a <strong>low-end system or limited RAM</strong>, ZRAM can be a game-changer. It provides a noticeable performance boost with minimal setup.</p> <p>Thanks for reading! I hope you found this post helpful.</p> <p>If you have any questions or suggestions, feel free to leave a comment below.</p> <p><strong>Next time</strong>, we’ll look at how some popular operating systems handle memory management and swap strategies.</p> <p>References:</p> <ul> <li>https://www.kernel.org/doc/html/latest/admin-guide/blockdev/zram.html</li> <li>https://dev.to/archerallstars/lets-fine-tune-your-zram-aka-free-ram-in-opensuse-with-zstandard-aka-zstd-4eap</li> </ul>]]></content><author><name></name></author><category term="utilities"/><category term="linux"/><summary type="html"><![CDATA[Sharing my experience with ZRAM on Linux—how it helped my low-RAM laptop feel faster by using compressed swap in RAM.]]></summary></entry><entry><title type="html">P2P - UDP Hole Punching</title><link href="https://www.dinhphu28.com/blog/2024/udp-hole-punching/" rel="alternate" type="text/html" title="P2P - UDP Hole Punching"/><published>2024-11-29T04:59:00+00:00</published><updated>2024-11-29T04:59:00+00:00</updated><id>https://www.dinhphu28.com/blog/2024/udp-hole-punching</id><content type="html" xml:base="https://www.dinhphu28.com/blog/2024/udp-hole-punching/"><![CDATA[<h3 id="introduction">Introduction</h3> <p>UDP hole punching is a technique for establishing bidirectional UDP connections between two networked devices behind NATs.</p> <p>But this technique is not always applicable because of multiple types of NATs.</p> <p>In this article, we will discuss the UDP hole punching concept and how it works.</p> <h3 id="what-is-nat">What is NAT?</h3> <p>Because of the limited availability of IPv4, Network Address Translation (NAT) was introduced to allow multiple devices to share a single public IP address.</p> <p>NAT remaps the private IP:port of devices in private networks to a public IP:port.</p> <p>There are multiple types of NATs, but for simplicity, we can categorize them into 2 types: Symmetric and anything else.</p> <ul> <li>Symmetric NAT remaps the public port depending on the endpoint.</li> <li>Other types of NATs are independent.</li> </ul> <h3 id="how-does-udp-hole-punching-work">How does UDP hole punching work?</h3> <p>This technique is originally to solve the firewall problem, because this problem is about NAT &amp; firewall. So we assume that the NAT is include firewall.</p> <p>Not as TCP, UDP is connectionless and doesn’t have a handshake mechanism. So when we send a packet out, it doesn’t care that it will reach the destination or not, and any packet coming back is considered as a response.</p> <p>Assume that we have known public IP:port of A and B.</p> <p>Idea of UDP hole punching is tricking NATs to allow incoming packets from the destination. There are 2 step to archive this:</p> <ul> <li>A sends a packet to B (this packet will be lost because B is also behind NAT)</li> <li>B sends a packet to A, because NAT considers this as a response, packet will be allowed to pass</li> </ul> <p>But problem of NAT is we don’t know the public port of A and B. So we need a third party to help us, this is a device at the public side can see both public side of A and B. It’s called STUN server.</p> <p>STUN server listens on a well-known port and when A and B want to establish a connection, they send a request to STUN server to get their public IP:port.</p> <p>Then A and B exchange their public IP:port and send a packet to each other to establish a connection.</p> <p>Because of endpoint independent, non-symmetric NAT always remaps the private IP:port to a fixed public IP:port, the port they get from STUN when sending request is the same when sending packet to each other.</p> <p>Let do a simple example with netcat:</p> <p>Server S’s IP: <em>14.123.124.125</em></p> <ol> <li>Server S listening on 3478/UDP (act as a STUN server)</li> </ol> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nc <span class="nt">-luvn</span> 3478
</code></pre></div></div> <ol> <li>Client A send a request to S on 3478/UDP</li> </ol> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nc <span class="nt">-vu</span> <span class="nt">-p</span> 50001 14.123.124.125 3478
</code></pre></div></div> <p>We can see the source IP:port is <strong><em>11.100.101.102:23241</em></strong></p> <ol> <li>Back to step 1</li> <li>Client B send a request to S on 3478/UDP</li> </ol> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nc <span class="nt">-vu</span> <span class="nt">-p</span> 50002 14.123.124.125 3478
</code></pre></div></div> <p>We can see the source IP:port is <strong><em>12.100.101.201:37261</em></strong></p> <p>Now we have known public IP:port of A and B</p> <p>Establish the P2P connection:</p> <ol> <li>A send a request to B on <strong><em>37261</em></strong></li> </ol> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nc <span class="nt">-vu</span> <span class="nt">-p</span> 50001 12.100.101.201 37261
</code></pre></div></div> <p>Nothing happened</p> <ol> <li>B send a request to A on <strong><em>23241</em></strong></li> </ol> <div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nc <span class="nt">-vu</span> <span class="nt">-p</span> 50002 12.100.101.201 23241
</code></pre></div></div> <p>Now A and B have established a connection. Type something on A, we can see it on B and vice versa.</p> <p>But this just solve the non-symmetric NAT, for symmetric which we cannot predict the public port, we need a relay server call TURN.</p> <p>This is the last resort when STUN technique doesn’t work.</p> <h3 id="stun-turn-and-ice">STUN, TURN and ICE</h3> <p>Interactive Connectivity Establishment (ICE) is a standard for using STUN and TURN to provide a solution for NAT traversal.</p> <p>They are used in WebRTC, VoIP and many P2P applications.</p> <p>Mesh VPN is a good example of using ICE to establish a P2P connection.</p> <h3 id="conclusion">Conclusion</h3> <p>In this article, we have discussed the concept of UDP hole punching and how it works, and what is STUN, TURN, ICE and how they resolve the NAT traversal problem.</p> <p>In case you have any questions, feel free to ask or share your opinion in the comment section.</p> <p>If you care about my VPN setup with Hub and Spoke, read <a href="/blog/2024/hub-spoke-vpn/">this article</a></p>]]></content><author><name></name></author><category term="networking"/><category term="p2p"/><category term="nat"/><category term="stun"/><category term="ice"/><category term="network"/><summary type="html"><![CDATA[UDP hole punching is a technique for establishing bidirectional UDP connections between two networked devices behind NATs.]]></summary></entry></feed>